<html>
  <head>
    <title>Loris</title>
    <body>
      Stadiums:
      <OL>
        <% data["stadiums"].each do |stadium| %>
        <LI>
           <%= stadium["name"] %>
        </LI>
        <br/>
        <% end %>
      </OL>
      Groups:
            <OL>
        <% data["groups"].each do |stadium| %>
        <LI>
           <%= groups["name"] %>
        </LI>
        <br/>
        <% end %>
      </OL>
    </body>
</html>
